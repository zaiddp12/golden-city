# تشغيل المدينة الذهبية على Railway

## رفع الملفات
فك الضغط وارفع محتويات الحزمة في جذر مستودع GitHub مع الحفاظ على مجلد static.
استبدل server.py بالنسخة المرفقة؛ أصلحت فتح الصفحة من رابط خارجي مع استمرار رفض طلبات API العابرة للمواقع.
الحزمة لا تحتوي قاعدة بيانات أو ملف زبائن أو وحدات وهمية.
requirements.txt يحتوي تعليقاً فقط: المشروع يستخدم مكتبات Python القياسية ولا يحتاج pip packages.
Dockerfile يستخدم Python 3.12؛ railway.json يحدد التشغيل والفحص الصحي تلقائياً.

## الإعداد
1. أنشئ مشروع Railway واربط مستودع GitHub.
2. أضف Volume مرتبطاً بالخدمة واجعل Mount Path هو /data.
3. للبدء بقاعدة جديدة فقط: أضف في Variables:
   INITIAL_ADMIN_USERNAME = اسم دخول إنكليزي مثل admin
   INITIAL_ADMIN_PASSWORD = كلمة مرور خاصة من 12 إلى 256 حرفاً.
4. اترك أمر التشغيل من الحزمة: python -u railway_start.py
   احذف أي أمر قديم من Render أو render_demo.py في إعدادات الخدمة.
5. انشر ثم Settings → Networking → Public Networking → Generate Domain.
6. سجل الدخول بالقيم التي اخترتها. احذف INITIAL_ADMIN_PASSWORD من Variables بعد نجاح إنشاء الحساب؛ إعادة التشغيل لا تعيد ضبط كلمة المرور أو تنشئ مديراً إضافياً.
7. إذا بدأت بقاعدة فارغة، استخدم استيراد Excel من داخل حساب المدير ثم راجع الاستيراد قبل اعتماده.

Railway يوفر PORT وRAILWAY_VOLUME_MOUNT_PATH تلقائياً؛ لا تضبط الأخير يدوياً كبديل عن Volume.
الخدمة ترفض البدء دون مسار Volume. قاعدة البيانات: /data/golden-city.sqlite3.
استخدم نسخة تشغيل واحدة فقط مع SQLite؛ لا تستخدم عدة replicas.

## نقل البيانات والحسابات الحالية
استيراد Excel لا ينقل الحسابات ولا سجل الإجراءات والحجوزات الجديدة.
للحفاظ على كل عملك الحالي، انقل نسخة SQLite متسقة من جهازك إلى Volume بشكل خاص قبل تشغيل التطبيق عليها.
أوقف السيرفر المحلي وخذ نسخة احتياطية؛ لا تنسخ ملف SQLite وحده أثناء الكتابة عليه.
استخدم SQLite backup API أو نسخة متسقة بعد الإغلاق. لا ترفع قاعدة البيانات أو كلمات المرور إلى GitHub.
يمكن إدارة ملفات Volume عبر Railway CLI: railway volume browse /
أوقف خدمة الويب المستهدفة قبل استبدال قاعدة موجودة وخذ نسخة منها أولاً؛ لا تستبدل قاعدة مستخدمة أثناء عمل الخدمة.
إذا نقلت قاعدة فيها مدير، تستخدم الحسابات القديمة؛ متغيرات INITIAL_ADMIN لا تغيرها.
تأكد من أعداد الوحدات والحجوزات وتسجيل الدخول قبل العودة للعمل على النسخة المستضافة.

## النسخ الاحتياطية وحدود التحقق
فعّل نسخ Volume الاحتياطية من Railway واحتفظ بنسخ SQLite متسقة دورية خارج الخدمة.
تم اختبار التهيئة، حفظ حساب المدير عبر إعادة التهيئة، تسجيل دخول HTTP وSecure cookie، ومنع cross-site API.
تم اختبار فتح الصفحة من رابط خارجي. لم يتم نشر هذه الحزمة على حساب Railway ولم يتم بناء Docker محلياً لأن Docker غير متاح.
هذه تهيئة نشر للخادم الحالي المبني على Python ThreadingHTTPServer؛ ليست إعادة بناء على خادم إنتاج WSGI/ASGI أو اختبار تحمل وأمان شامل للإنترنت.

المراجع:
https://docs.railway.com/volumes
https://docs.railway.com/networking/public-networking
